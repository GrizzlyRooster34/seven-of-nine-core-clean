import { EventEmitter } from 'events';
import { Trace } from '../db/spark-db.types';
export declare class SparkEngine extends EventEmitter {
    private dbPath;
    private db;
    private SQL;
    private codex;
    private beliefs;
    private selfModel;
    private tickInterval;
    private tickCount;
    private isRunning;
    private isInitialized;
    private sensorBridge;
    private readonly CANDIDATE_INTENTIONS;
    constructor(dbPath?: string);
    initialize(): Promise<void>;
    private saveDatabase;
    private loadSelfModel;
    private saveSelfModel;
    private logBoot;
    private getPowerState;
    private getCpuUsage;
    private getBatteryLevel;
    start(): Promise<void>;
    private scheduleNextTick;
    stop(): Promise<void>;
    private tick;
    private generateSparkApproval;
    private sense;
    private updateBeliefs;
    private scoreIntention;
    private checkRails;
    private act;
    private writeTrace;
    private generateTraceNote;
    getRecentTraces(limit?: number): Promise<Trace[]>;
    close(): Promise<void>;
}
export default SparkEngine;
