# Google Cloud Platform Configuration
# Edit these values with your GCP project details
# DO NOT commit this file to git (already in .gitignore)

# ============================================
# REQUIRED: Project Configuration
# ============================================
export GCP_PROJECT_ID="s-o-n-core"
export GOOGLE_CLOUD_PROJECT="s-o-n-core"
export GCP_REGION="us-central1"
export GOOGLE_CLOUD_LOCATION="us-central1"
export GOOGLE_APPLICATION_CREDENTIALS="$HOME/vertex-key.json"

# ============================================
# Cloud Run Resource Configuration
# ============================================
export MEMORY="2Gi"           # Memory: 512Mi, 1Gi, 2Gi, 4Gi, 8Gi
export CPU="2"                # CPU: 1, 2, 4, 8
export MAX_INSTANCES="10"     # Maximum autoscale instances
export MIN_INSTANCES="0"      # Minimum instances (0 = scale to zero)
export TIMEOUT="300s"         # Request timeout (max 3600s)

# ============================================
# Application Environment
# ============================================
export NODE_ENV="production"
export LOG_LEVEL="info"

# ============================================
# Optional: Database Configuration
# ============================================
# Uncomment if using Cloud SQL
# export CLOUDSQL_INSTANCE="PROJECT_ID:REGION:INSTANCE_NAME"
# export DB_HOST="/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME"
# export DB_USER="seven-user"
# export DB_PASSWORD="your-secure-password"
# export DB_NAME="seven_db"

# ============================================
# Optional: Secrets
# ============================================
# WARNING: Use Google Secret Manager in production!
# These are for development/testing only
# export ANTHROPIC_API_KEY="your-api-key"
# export OPENAI_API_KEY="your-api-key"
# export ENCRYPTION_KEY="your-encryption-key"

# ============================================
# Optional: Monitoring
# ============================================
# export GCP_LOG_LEVEL="INFO"
# export ENABLE_PROFILING="false"
# export ENABLE_TRACING="false"

# ============================================
# Quick Setup Instructions
# ============================================
# 1. Replace YOUR_PROJECT_ID_HERE with your actual GCP project ID
# 2. Adjust region if needed (us-central1, us-east1, europe-west1, etc.)
# 3. Adjust resources based on your needs
# 4. Source this file: source .env.gcp
# 5. Run setup: ./scripts/setup-gcp.sh
# 6. Deploy: ./scripts/deploy-gcp.sh
